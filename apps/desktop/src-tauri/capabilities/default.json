{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline permissions for the main window. No remote IPC access is granted to pharmatrack.co.ke in v1 - the window is a plain webview, all native behavior (tray, single-instance, window-state, updater) is driven from Rust, not invoked from the web page.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
